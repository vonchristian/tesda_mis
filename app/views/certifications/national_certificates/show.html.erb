<div class="row">
  <div class="col-md-12">
    <div class="box box-solid">
      <div class="box-header with-border">
        <h3 class="box-title"> <%=@national_certificate.number %> 
        
          <span class="pull-right">
            <%=link_to national_certificate_path(@national_certificate, format: 'pdf') do %>
              <span class="btn btn-default"><i class="fa fa-file-pdf-o"></i> View (PDF) </span>
            <% end %>
          </span>
        </h3>
      </div>
      <div class="box-body">
      <%=link_to "New Issuance", new_certification_issuance_path(@national_certificate), class: "btn btn-default btn-sm" %>
        <%= @national_certificate.client_full_name %>
        <%= @national_certificate.qualification_name %>
        <%= @national_certificate.issue_date.strftime("%B %e, %Y") %>
        <%= @national_certificate.expiry_date.strftime("%B %e, %Y") %>
        <%= @national_certificate.number %>
        <p class="text-muted"> Assessor: </p>
        <%= @national_certificate.assessor_full_name %>
      </div>
    </div>
  </div>
</div>
